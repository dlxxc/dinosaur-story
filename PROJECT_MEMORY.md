# 恐龙教养故事网站 - 项目记忆文档

> 本文档为项目的核心需求与规则沉淀，所有后续开发必须以此为准。
> 文档生成日期：2026-08-10
> 产品定位：专为 4-6 岁儿童设计的教养故事网站，主角为拟人化恐龙，点击文字触发语音朗读，在听故事中自然习得礼貌与好习惯。

---

## 一、开发军规

以下军规在后续所有对话中自动生效，无需重复说明：

1. **分步验收**：开发必须切分成独立的阶段，每完成一个阶段要停下来找我验收，严禁一口气写完所有代码。
2. **方案抉择**：遇到技术难题，必须给出至少 2 个实现方案，并用大白话解释优缺点，等我拍板后再动手。
3. **测试标准**：只需为核心流程写最简单的测试（能跑通就行），不用追求高覆盖率。
4. **变更准则**：当我后续提出新想法时，如果与现有功能冲突，必须先提示我波及范围，并询问是"修改旧的"还是"新建模块"，严禁直接覆盖。
5. **依赖管理**：新增第三方库前必须告诉我用途和替代方案，经我同意后才能安装，禁止擅自引入臃肿或不安全的依赖。
6. **错误处理**：所有用户操作必须有明确的成功/失败反馈（弹窗或提示文字），禁止"静默失败"——我作为非技术用户无法看日志排错。
7. **文档同步**：每完成一个阶段，同步更新 PROJECT_MEMORY.md 中对应章节的状态（标记为"已完成"），保证外部记忆与代码一致。
8. **禁止过度设计**：严格按照确认的需求实现，不要自行添加未讨论过的功能。如果发现需要补充的功能，先提出建议等我确认，不要直接写进代码。

---

## 二、产品概览

| 项目 | 内容 |
| :--- | :--- |
| 产品名称 | 恐龙教养故事网站（暂定） |
| 目标用户 | 4-6 岁儿童（由家长陪同或自主使用） |
| 使用场景 | 家长用手机（主要为荣耀手机）打开网站给孩子听故事 |
| 核心价值 | 听故事过程中自然习得礼貌与好习惯 |
| 主角设定 | **拟人化的恐龙角色**（硬性内容约束，所有故事主角必须是拟人化恐龙） |
| 用户规模 | 单用户使用（只给一个孩子用，无需多账号体系） |

---

## 三、功能需求清单

### 3.1 首页 - 故事列表

| 功能点 | 说明 |
| :--- | :--- |
| 首页内容 | 直接展示故事列表（无欢迎页/封面页） |
| 分类方式 | 按"礼貌篇 / 习惯篇 / 分享篇"三大主题分类 |
| 分类展示形态 | **折叠显示**：点分类才展开下面的故事，更整洁但多一步操作 |
| 故事卡片展示 | 标题 + 一张小插图缩略图 |
| 已读完标记 | 读过的故事显示"已读完"标记 |
| 重置进度按钮 | 放在故事列表页的角落（孩子不易误触） |
| 重置确认 | 点击后弹窗提示"确定要清空所有进度吗？"，确认后才执行 |

> 【待确认】故事列表页的具体框架布局（卡片排列方式、每行几个、分类折叠的交互细节等）——用户表示后期要人工确认。

### 3.2 故事详情页

| 功能点 | 说明 |
| :--- | :--- |
| 页面布局 | 上图下文，主要适配手机竖屏 |
| 插图形式 | **多张插图，分段配图，绘本式翻页**（每个段落配一张图） |
| 文字显示 | 一整段显示（非逐句切分显示） |
| 朗读高亮 | 朗读时，读到哪一句，那一句高亮变色 |
| 插图要求 | 每个故事都必须有插图，不允空白 |
| 字号调节 | 支持小 / 中 / 大 三档切换 |

> 【待确认】
> - 每个故事具体分几段、配几张插图（属于内容阶段确认）。
> - 字号三档的默认值是哪一档。
> - "绘本式翻页"的具体交互（是滚动向下看，还是左右滑动翻页）。

### 3.3 语音朗读功能

| 功能点 | 说明 |
| :--- | :--- |
| 语音来源 | **浏览器自动合成语音**（文字转语音，无需提前录音频） |
| 触发方式 | 点击"播放按钮"开始从头读到尾 |
| 暂停功能 | 支持暂停 |
| 语速调节 | 3 档：快 / 适中 / 慢 |
| 朗读结束行为 | 读完自动停止（不自动跳下一个） |
| 断点续读 | 不支持。关闭浏览器后再打开同一故事，**从头读** |
| 兼容性要求 | 主要在荣耀手机自带浏览器播放，必须保证可用 |

> 【待确认】语速三档的默认值是哪一档。

### 3.4 故事阅读完成后的操作

| 功能点 | 说明 |
| :--- | :--- |
| 再听一遍 | 支持 |
| 跳到下一个故事 | 支持 |
| 已读完标记 | 读完自动标记，关闭浏览器后仍保留 |

> 【待确认】"跳到下一个故事"的规则——是跳同分类下的下一个，还是所有故事按顺序的下一个？

### 3.5 离线下载功能

| 功能点 | 说明 |
| :--- | :--- |
| 下载入口 | 每个故事旁边各有一个"下载"按钮（非一键下载全部） |
| 下载对象 | 故事的插图 + 朗读所需内容 |
| 下载进度 | 显示"下载中 XX%"、"下载完成" |
| 下载失败 | 弹窗提示"下载失败" |
| 离线使用 | 下载完成后，无网络也可听该故事 |

> 【待确认】
> - 已下载的故事如何在列表中标识（比如加个"已下载"角标）。
> - 已下载内容如何管理（能否删除已下载内容释放空间）。
> - 下载内容存储位置（浏览器本地缓存）。

### 3.6 故事内容管理

| 功能点 | 说明 |
| :--- | :--- |
| 内容来源 | 由程序员在开发阶段提前写好（非用户上传） |
| 后续新增 | 需找程序员改代码新增（非自助后台） |
| 故事长度 | 几百字的小故事（朗读约 3-5 分钟） |
| 主角约束 | **所有故事主角必须是拟人化的恐龙** |
| 主题分类 | 礼貌篇 / 习惯篇 / 分享篇 |

> 【待确认】具体的故事清单（标题、归属分类、分段内容、插图描述）——用户表示后面再逐篇确认。

---

## 四、非功能需求

### 4.1 设备兼容性

| 设备类型 | 支持情况 |
| :--- | :--- |
| 荣耀手机自带浏览器 | **必须支持**（主要使用场景） |
| 华为等国产手机浏览器 | 需要支持 |
| 电脑浏览器 | 需要支持（家里电脑打开看） |
| 苹果手机 Safari | 【待确认】用户未明确，需后续确认 |

### 4.2 权限与安全

| 项目 | 说明 |
| :--- | :--- |
| 访问方式 | 公开访问，任何人知道网址即可进入 |
| 登录 | 不需要登录 |
| 管理员/普通用户 | 无区分（单用户产品） |

### 4.3 数据存储

| 数据 | 存储方式 | 持久性 |
| :--- | :--- | :--- |
| 已读完标记 | 浏览器本地存储 | 关闭浏览器后仍保留 |
| 字号偏好 | 【待确认】是否记忆用户上次选择的字号 |
| 语速偏好 | 【待确认】是否记忆用户上次选择的语速 |
| 离线下载内容 | 浏览器本地缓存 | 关闭浏览器后仍保留 |
| 故事内容 | 程序员写死在代码里 | 永久 |

> 说明：因为是单用户产品且无需登录，所有数据均存在本地浏览器，不需要服务器端数据库。

---

## 五、待确认事项汇总

| 编号 | 待确认内容 | 说明 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | 故事列表页的具体框架布局 | 每行 1 个卡片，上下排列 | ✅ 已确认 |
| 2 | 故事具体清单（标题/分类/分段/插图描述） | 内容阶段逐篇确认 | ⏳ 待确认 |
| 3 | "绘本式翻页"的具体交互 | 上下滚动（像长文章） | ✅ 已确认 |
| 4 | 字号三档的默认值 | 默认"大" | ✅ 已确认 |
| 5 | 语速三档的默认值 | 默认"适中" | ✅ 已确认 |
| 6 | "跳到下一个故事"的规则 | 跳同分类下的下一个 | ✅ 已确认 |
| 7 | 已下载故事的标识方式 | 是否加角标 | ⏳ 待确认 |
| 8 | 已下载内容的管理 | 能否删除 | ⏳ 待确认 |
| 9 | 字号/语速偏好是否记忆 | 下次打开是否保留 | ⏳ 待确认 |
| 10 | 苹果手机 Safari 兼容性 | 用户未明确 | ⏳ 待确认 |
| 11 | 多张插图的具体数量 | 每段几张 | ⏳ 待确认 |

---

## 六、开发阶段规划（草案，待技术栈确认后细化）

> 此处仅为规划框架，具体阶段拆分待技术栈选型确认后再细化。

1. **阶段一**：技术栈确认 + 项目结构搭建 + 隔离环境创建
2. **阶段二**：静态页面框架（首页 + 故事详情页骨架）
3. **阶段三**：语音朗读功能 + 朗读高亮
4. **阶段四**：字号/语速调节 + 已读完标记 + 重置功能
5. **阶段五**：离线下载功能
6. **阶段六**：内容填充（故事文本 + 插图）—— 需逐篇与用户确认
7. **阶段七**：兼容性测试（荣耀/华为/电脑浏览器）
8. **阶段八**：验收与部署

---

## 七、变更记录

| 日期 | 变更内容 | 操作人 |
| :--- | :--- | :--- |
| 2026-08-10 | 初始需求访谈完成，生成首版 PROJECT_MEMORY.md | 产品经理 + CTO |

---

## 八、当前状态

- [x] 需求访谈完成
- [x] 技术栈选型确认（Vue 3 + Vite）
- [x] 项目结构搭建
- [x] 隔离环境创建（Node 本地依赖，已安装 36 个包，0 漏洞）
- [x] 阶段一验收通过
- [ ] 阶段二：静态页面框架开发
- [ ] 阶段三：语音朗读功能
- [ ] 阶段四：字号/语速调节 + 已读完标记 + 重置功能
- [ ] 阶段五：离线下载功能
- [ ] 阶段六：内容填充
- [ ] 阶段七：兼容性测试
- [ ] 验收部署
